<?xml version="1.0" encoding="utf-8" ?>
<TrustFrameworkPolicy 
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
  xmlns:xsd="http://www.w3.org/2001/XMLSchema" 
  xmlns="http://schemas.microsoft.com/online/cpim/schemas/2013/06" 
  PolicySchemaVersion="0.3.0.0" 
  TenantId="{tenantName}.onmicrosoft.com" 
  PolicyId="B2C_1A_TrustFrameworkExtensions" 
  PublicPolicyUri="http://{tenantName}.onmicrosoft.com">
  
  <BasePolicy>
    <TenantId>{tenantName}.onmicrosoft.com</TenantId>
    <PolicyId>B2C_1A_TrustFrameworkBase</PolicyId>
  </BasePolicy>
  <BuildingBlocks>

  </BuildingBlocks>

  <ClaimsProviders>

    <ClaimsProvider>
      <DisplayName>Facebook</DisplayName>
      <TechnicalProfiles>
        <TechnicalProfile Id="Facebook-OAUTH">
          <Metadata>
		  <!--Replace the value of client_id in this technical profile with your the Facebook App ID"-->
            <Item Key="client_id">1615sample706330</Item>
            <Item Key="scope">email public_profile</Item>
            <Item Key="ClaimsEndpoint">https://graph.facebook.com/me?fields=id,first_name,last_name,name,email</Item>
          </Metadata>
        </TechnicalProfile>
      </TechnicalProfiles>
    </ClaimsProvider>


    <ClaimsProvider>
      <DisplayName>Local Account SignIn</DisplayName>
      <TechnicalProfiles>
        <TechnicalProfile Id="login-NonInteractive">
          <Metadata>
		  
		  <!-- This is the client id of the native app PolicyEngineProxy in your tenant-->          	
            <Item Key="client_id">{Policy Engine Proxy Application ID}</Item>
		  
		  <!-- This is the client of of the web application PolicyEngine in the your tenant on which the above native application has been granted delegate permissions -->
            <Item Key="IdTokenAudience">{Policy Engine Application ID}</Item>
          </Metadata>
		  
          <InputClaims>
		   <!-- This is the client id of the native app PolicyEngineProxy in your tenant -->
            <InputClaim ClaimTypeReferenceId="client_id" DefaultValue="{Policy Engine Proxy Application ID}" />
			
			<!-- This is the client of of the web application PolicyEngine in your tenant on which the above native application has been granted delegate permissions -->
            <InputClaim ClaimTypeReferenceId="resource_id" PartnerClaimType="resource" DefaultValue="{Policy Engine Application ID}" />
          </InputClaims>
        </TechnicalProfile>
      </TechnicalProfiles>
    </ClaimsProvider>

  </ClaimsProviders>

    <!--UserJourneys>
	
	</UserJourneys-->

</TrustFrameworkPolicy>
